{
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix packages/shared-types && npm install --prefix frontend"
  },
  "name": "email-imap-client",
  "version": "1.0.0",
  "author": "",
  "license": "ISC",
  "description": ""
}